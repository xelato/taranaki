module Main


provide let collatz = (x: Number) => {
    let mut x = x
    let mut count = 0
    while (x>1) {
        count += 1
        if (x % 2 == 0) {
            x = x/2
        } else {
            x = 3 * x + 1
        }
    }
    count += 1
    count
}

print(collatz(871))
